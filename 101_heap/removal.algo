1) If the node you want to remove is the root, remove it
2) If the node you want to remove is the left child of the root, remove it
  a) If the node you want to remove is less than the left child of the root, repeat these steps on the right child of the root
  b) If the node you want to remove is greater than the left child of the root, repeqt these steps on the left child of the root
